# Chezmoi itself
/.config/chezmoi/
/.local/share/chezmoi/

# Personal files
/Desktop/
/Documents/
/Downloads/
/Music/
/Pictures/
/Public/
/Templates/
/Videos/
/.local/share/Trash/

# Temporary files
/.cache/

# History files
/.bash_history
/.lesshst
/.viminfo
/.config/katemetainfos

# Files managed by scripts
/.config/dconf/user
/.config/dconf/user.ini

# Secrets that can be safely regenerated
/.config/kdeconnect/certificate.pem
/.config/kdeconnect/privateKey.pem
/.local/share/krdpserver/kdrp.crt
/.local/share/krdpserver/kdrp.key

# Temporary/session-specific data
/.config/pulse/cookie
/.config/session/
/.config/kconf_updaterc
# TODO: Will (probably) still need to check .local/share for useful data, but the majority of it isn't useful
/.local/share/
/.local/state/
/.var/

# Accounts SQLite database (TODO: figure out what this is and whether it should be stored)
/.config/libaccounts-glib/

# Firefox data, ignored by default because most of it is personal data
/.mozilla/firefox/*
!/.mozilla/firefox/*.ini
!/.mozilla/firefox/axn5gnby.default-release/
/.mozilla/firefox/axn5gnby.default-release/*
# TODO: most of the settings in prefs.js are unnecessary or personal. Maybe set up a user.js, or just ignore and rely on Firefox Sync.

# Files automatically generated by KDE
/.config/gtkrc
/.config/gtkrc-2.0
